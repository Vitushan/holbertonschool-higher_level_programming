#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    """
    fonction qui replace la liste
    """
    if idx < 0 or idx >= len(my_list):
        return my_list
    my_list[idx = element]
