#!/usr/bin/python3
def matrix_divided(matrix, div):
    if not isinstance(matrix(int, float)):
        raise TypeError("matrix must be a matrix (list of lists) of integers/floats")
    all_element =len(matrix[i])
    return all_element
