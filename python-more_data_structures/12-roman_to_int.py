#!/usr/bin/python3
def roman_to_int(roman_string):
    if not roman_string or if isinstance(roman_string, str):
        print(f"{roman_string} is not a string.")
        return 0
    else:
        print(f"{roman_string} is a string.")
    return 1
